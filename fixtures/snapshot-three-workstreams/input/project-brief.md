# Enterprise rollout brief — synthetic

The enterprise pilot cannot start until security completes the data-retention
review. The implementation is ready; the review is the active dependency.

We chose three rollout cohorts rather than one broad launch because the support
rotation has one on-call pair. Staging the rollout keeps each cohort within the
team's response capacity and creates a pause point after every cohort.

**Manager ask:** choose the first cohort by Friday, 2026-08-28, so enablement can
send the onboarding schedule.
